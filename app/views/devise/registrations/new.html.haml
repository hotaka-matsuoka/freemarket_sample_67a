.wrapper
  .signup-bucket
    = render  'devise/shared/progress_header' 
  %main.single-main 
    %section.l-single-bucket
      %h2.single-bucket__registration 会員情報入力
      = form_for(@user, url: user_registration_path)  do |f|
        = render "devise/shared/error_messages", resource: @user
        .l-single-bucket__nickname
          = f.label :ニックネーム
          %span.form-require 必須
          %br/
          = f.text_field :nickname, autofocus: true, placeholder: "例）フリマ太郎"
        .l-single-bucket__email
          = f.label :メールアドレス
          %span.form-require 必須
          %br/
          = f.email_field :email, autocomplete: "email", placeholder: "PC・携帯どちらでも可"
          
        .l-single-bucket__password
          = f.label :パスワード
          %span.form-require 必須
          %br/
          %p.password__text※英字と数字の両方を含めて設定してください
          = f.password_field :password, autocomplete: "new-password", placeholder: "7文字以上の半角英数字"
        .l-single-bucket__password__confimation
          = f.label :確認パスワード
          %span.form-require 必須
          %br/
          = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "再度パスワードを入力してください"

        = f.label :お名前（全角）
        %span.form-require 必須
        %br/
        .l-single-bucket__name__form
          = f.text_field :family_name, placeholder: "例）山田"
          = f.text_field :first_name, placeholder: "例）彩"
        .l-single-bucket__name__kana
        = f.label :お名前カナ（全角）
        %span.form-require 必須
        %br/
        .l-single-bucket__name__kana
          = f.text_field :family_name_kana, placeholder: "例）ヤマダ"
          = f.text_field :first_name_kana, placeholder: "例）アヤ"
        .l-single-bucket__birthday
          = f.label :生年月日
          %span.form-require 必須
          %br/
          .birthday-select-wrap
            = f.date_select :birth_day, use_month_numbers: true, start_year: 1930, end_year: (Time.now.year ), include_blank: true
            %p.form-into-text ※ 本人情報は正しく入力してください。
            会員登録後、修正するにはお時間を頂く場合があります。
        .l-single-bucket__content
          .l-single-bucket__content__center
            %p.l-single-bucket__content__center__text「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします
          .actions
            = f.submit "次へすすむ",class:'next-btn'
          .l-single-bucket__content__text
            =link_to"#" do
              本人情報の登録について>
          .l-single-bucket__content__policy
          %p.l-single-bucket__content__policy__text
            This site is protected by reCAPTCHA and the Google Privacy Policy and Terms of Service apply.
          
  .footer-bucket
    .single-footer
      .signup__footer__detail
        = link_to "#"do
          プライバシーポリシー
        = link_to "#"do
          furima利用規約
        = link_to "#"do
          特定商取引に関する表記
      .signup__footer__logo
      =link_to"#"do
        <img src="/assets/logo/logo.png" width="160" height="46">
      %p © FURIMA,inc